package sample.Tests;

/*import org.junit.Assert;
import org.junit.Before;
import org.junit.Test;
import sample.Enumeration.TypeCard;
import sample.Model.CardModel;
import sample.Model.Player;


 * Created by theo on 09/12/16.



public class PlayerTests {
    private Player p = new Player();

    @Before
    public void initialise(){
    }

    @Test
    public void testAddRemove(){
        CardModel cM = new CardModel(0, TypeCard.Club);
        p.addCardsToAPlayer(cM);
        Assert.assertEquals(p.getCards().get(0), cM);
        Assert.assertEquals(p.getCards().size(), 1);
        p.removeCardsToAPlayer(cM);
        Assert.assertEquals(p.getCards().size(),0);
    }
}
*/